(function() {
  $('#items').html('<%= j render items(DateRange.new(resource.source.date).month) %>');

  $('#at_end').html('<%= money Cash.at_end %>');

  $('#balance').html('<%= money Cash.balance %>');

  $('#consolidates').html('<%= j render consolidates %>');

  $('#item_<%= resource.id %>').remove();

  $('.modal').modal('hide');
}());
