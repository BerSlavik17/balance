(function() {
  $('#item_form').replaceWith('<%= j render 'form', resource: Item.new %>');

  $('.date').datepicker();

  $('#items').replaceWith('<%= j render items(DateRange.new(resource.source.date).month) %>');
}());
