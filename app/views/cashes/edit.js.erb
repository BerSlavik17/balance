(function() {
  var $dialog = $('#dialog');

  if($dialog.length < 1) {
    var dialog = document.createElement('div');

    dialog.id = 'dialog';

    $dialog = $(dialog);
  }

  $dialog.html('<%= j render 'form' %>').dialog();
}());
