$('#cash_form').html('<%= escape_javascript(render 'form', :cash => @cash) %>').dialog({
  autoOpen: true,
  modal: true,
  title: 'Править остаток "<%= @cash.name %>"',
  width: '600px'
})

$('#cash_sum').focus();
