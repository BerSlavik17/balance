<%= form_for resource, :remote => true do |f| %>
  <%= f.text_field :name %>
  
  <%= f.text_field :sum %>
  
  <%= f.submit %>

  <% unless resource.new_record? %>
    или 
    <%= link_to 'Удалить', resource, :method => :delete, :remote => true %>
  <% end %>
<% end %>
