<%= simple_form_for resource, remote: true, wrapper: :horizontal_form do |f| %>
  <%= f.input :sum, as: :string %>

  <%= f.input :name %>

  <div class="form-group row">
    <div class="col-sm-10 col-sm-offset-2">
      <%= f.button :submit, class: 'btn btn-primary' %>
    </div>
  </div>
<% end %>
