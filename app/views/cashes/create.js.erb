$('#cashes').html("<%= escape_javascript(render 'cashes', { :cashes => Cash.scoped }) %>");

$('#at_end').html("<%= sum Cash.at_end %>")
$('#balance').html("<%= sum Cash.balance %>")

$('form#for_cash').dialog('close')
