(function() {
  $('#cashes').replaceWith('<%= j render 'cashes', cashes: collection %>')

  $('#at_end').html("<%= money Cash.at_end %>")

  $('#balance').html("<%= money Cash.balance %>")

  $('#dialog').dialog('close')
}());
