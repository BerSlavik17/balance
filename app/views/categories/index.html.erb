<div class="container">
  <table class="table table-bordered">
    <tr>
      <th>ID</th>
      <th>Name</th>
    </tr>

    <% collection.each do |category| %>
      <tr>
        <td><%= category.id %></td>
        <td><%= category.name %></td>
      </tr>
    <% end %>
  </table>
</div>
